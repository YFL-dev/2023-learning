module.exports = {
  verbose: true, // Display detailed information for each test
  collectCoverage: true, // Collect coverage information
  coverageDirectory: 'coverage', // Directory where Jest should output coverage files
  testEnvironment: 'node', // Environment in which the tests will be run
};
